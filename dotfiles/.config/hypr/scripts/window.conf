#windowrule = float,class:^(thunar)$,title:^(rename)$
#hyprctl clients
windowrulev2 = float,class: (com.network.manager|blueman-manager|org.pulseaudio.pavucontrol|nm-connection-editor|xdg-desktop-portal-gtk)
#windowrulev2 = opacity 0.75 0.75, class:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

layerrule = blur, notifications
layerrule = ignorezero, notifications
#layerrule = blur, walker 
#layerrule = ignorealpha 0.5, walker
#layerrule = dimaround, walker

#windowrulev2 = noblur, class:(kitty)