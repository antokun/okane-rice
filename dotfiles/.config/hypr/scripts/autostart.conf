exec-once = systemctl --user start hyprpolkitagent
exec-once = /usr/lib/gnome-keyring/gnome-keyring-daemon --start

exec-once = hyprpaper &
exec-once = sleep 1 && ~/.config/hypr/scripts/random-wallpaper.sh

exec-once = waybar &
exec-once = blueman-applet

exec-once = systemctl --user start elephant.service
exec-once = walker --gapplication-service

#jalankan sebelum daemon thunar dijalankan
exec-once = rclone lsd gdrive1:
exec-once = thunar --daemon

exec-once = hypridle

#comment